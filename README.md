# Have fun


# Adding cadical SAT solver
In parent directory add git repo of cadical
``` bash
git clone https://github.com/arminbiere/cadical.git
cd cadical

./configure
make
```

Add `"../cadical/**"` to the vs code cpp property includePath  